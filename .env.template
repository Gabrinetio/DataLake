# Copie este arquivo para `.env` e preencha os valores reais (NÃO commite o `.env`)

# Hive Metastore
HIVE_DB_HOST=localhost
HIVE_DB_PORT=3306
HIVE_DB_NAME=metastore
HIVE_DB_USER=hive
HIVE_DB_PASSWORD=CHANGEME_HIVE_DB_PASSWORD

# Proxmox Host
PROXMOX_HOST=192.168.4.25
PROXMOX_USER=root
# Use chave canônica para acesso ao Proxmox (recomendado)
PROXMOX_SSH_KEY_PATH=scripts/key/ct_datalake_id_ed25519
# PROXMOX_PASSWORD está obsoleto; use apenas como fallback se absolutamente necessário
PROXMOX_PASSWORD=CHANGEME_PROXMOX_PASSWORD (deprecated)

# Airflow Container
AIRFLOW_CT_IP=192.168.4.36
AIRFLOW_CT_USER=datalake
AIRFLOW_WEB_PORT=8089
AIRFLOW_ADMIN_PASSWORD=CHANGEME_AIRFLOW_ADMIN

# SSH Keys
SSH_KEY_PATH=scripts/key/ct_datalake_id_ed25519
GITEA_TOKEN=CHANGEME_GITEA_TOKEN

# Superset Database
SUPERSET_DB_USER=superset
SUPERSET_DB_PASSWORD=CHANGEME_SUPERSET_DB_PASSWORD
SUPERSET_SECRET_KEY=CHANGEME_SUPERSET_SECRET_KEY

# MinIO
MINIO_ACCESS_KEY=CHANGEME_MINIO_ACCESS
MINIO_SECRET_KEY=CHANGEME_MINIO_SECRET

# Spark / Kafka
SPARK_TOKEN=CHANGEME_SPARK_TOKEN
KAFKA_PASSWORD=CHANGEME_KAFKA_PASSWORD

# Postgres (Hive)
POSTGRES_HIVE_PASSWORD=CHANGEME_POSTGRES_HIVE

# Nota: crie .env localmente a partir deste arquivo e não o commit.
